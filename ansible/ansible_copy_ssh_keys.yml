---
- name: Copy SSH keys to ESXi hosts
  hosts: esxi
  gather_facts: no
  become: no

  tasks:
    - name: Copy SSH key to authorized_keys
      shell: cat ~/.ssh/id_rsa.pub | ssh root@{{ ansible_host }} 'cat >> /etc/ssh/keys-root/authorized_keys'
      delegate_to: localhost
