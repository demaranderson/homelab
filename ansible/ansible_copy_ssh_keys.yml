- name: Copy SSH key to ESXi hosts
  hosts: esxi
  connection: local
  gather_facts: no
  become: no

  tasks:
    - name: Copy SSH key to authorized_keys
      ansible.builtin.shell: yes | ssh root@{{ ansible_host }} 'cat >> /etc/ssh/keys-root/authorized_keys' < /root/.ssh/id_rsa.pub
